/.bundle/
# rails_head.gemfile tracks a git branch, so its resolution is expected to drift; the
# other lockfiles are committed (see `rake lock:refresh`).
/gemfiles/rails_head.gemfile.lock
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
/spec/reports/
/tmp/
/docs/
/.compound-engineering/
CONCEPTS.md

# rspec failure tracking
.rspec_status
